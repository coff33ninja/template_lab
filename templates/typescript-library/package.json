{
  "name": "{{project_slug}}",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
