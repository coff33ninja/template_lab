# {{project_name}}

Multi-project idea lab.

## Suggested flow

1. Add quick idea notes in `ideas.md`.
2. Build first pass in `experiments/`.
3. Promote stable work into `backend/`, `mobile/`, or `cli/`.
