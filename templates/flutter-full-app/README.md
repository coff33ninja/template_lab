# {{project_name}}

Project slug: {{project_slug}}

Flutter full-platform scaffold.

This template is designed to materialize all platform folders.

## Materialize full app

```powershell
flutter create --project-name {{project_module}} --platforms=android,ios,web,windows,linux,macos .
flutter pub get
```

If you use `new-project.ps1 -InstallDeps`, this materialization runs automatically.
