# {{project_name}}

Fullstack monorepo scaffold.

## Workspaces

- `apps/api` TypeScript API
- `apps/web` static web app
- `apps/mobile` placeholder for Flutter app
- `packages/shared` shared TS utilities

## Run

```powershell
npm install
npm run test -ws --if-present
```
